name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch: # Allow manual triggers

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Generate Android directory if missing
        run: |
          if [ ! -d "android" ]; then
            echo "Android directory not found, generating native code..."

            # Create temp project outside current repo
            cd /tmp
            npx react-native@latest init TempProject --skip-install

            # Move android directory to the repo
            cd $GITHUB_WORKSPACE
            mv /tmp/TempProject/android ./

            # Clean up temp project
            rm -rf /tmp/TempProject

            # Update package name in android files
            find android -type f \( -name "*.java" -o -name "*.kt" -o -name "*.xml" -o -name "*.gradle" \) -exec sed -i 's/com\.tempproject/com.squircle/g' {} +

            echo "Android directory generated successfully"
          else
            echo "Android directory already exists"
          fi

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build Android APK
        run: cd android && ./gradlew assembleRelease

      - name: Build Android AAB (Play Store)
        run: cd android && ./gradlew bundleRelease

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Upload AAB artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: android/app/build/outputs/bundle/release/app-release.aab
          retention-days: 30

      - name: Create Release Summary
        run: |
          echo "## 📱 Android Build Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Download Options:" >> $GITHUB_STEP_SUMMARY
          echo "- **APK** (Direct install): Download from artifacts above" >> $GITHUB_STEP_SUMMARY
          echo "- **AAB** (Google Play): Upload to Play Console" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### To Install APK on Your Phone:" >> $GITHUB_STEP_SUMMARY
          echo "1. Download the APK artifact from this workflow run" >> $GITHUB_STEP_SUMMARY
          echo "2. Transfer to your Android phone" >> $GITHUB_STEP_SUMMARY
          echo "3. Enable 'Install from Unknown Sources' in Settings" >> $GITHUB_STEP_SUMMARY
          echo "4. Open the APK file to install" >> $GITHUB_STEP_SUMMARY
